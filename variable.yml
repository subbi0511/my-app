---
 - name: this play for varaiable declaration
   hosts: localhost
   vars:
   gather_facts: no
   tasks:
           - name: display variable value
             debug:
                     msg: 
                       - "the value of the x is: {{x}}"
                         #  - "the value of the y is: {{y}}" 

